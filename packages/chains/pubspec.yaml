name: chains
description: Chain primitives, transaction codecs, verification, and RPC contracts for KT Wallet.
version: 1.0.0
repository: https://github.com/siliconnexus-jp/KT-Wallet

environment:
  sdk: ^3.12.2

# Add regular dependencies here.
dependencies:
  cryptography: ^2.7.0
  pointycastle: ^4.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

resolution: workspace
