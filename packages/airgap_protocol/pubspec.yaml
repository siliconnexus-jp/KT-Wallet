name: airgap_protocol
description: Versioned CBOR and animated-QR protocol for KT Wallet air-gap signing.
version: 1.0.0
repository: https://github.com/siliconnexus-jp/KT-Wallet

environment:
  sdk: ^3.12.2

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

resolution: workspace
