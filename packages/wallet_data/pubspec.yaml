name: wallet_data
description: KT Wallet online-app persistence (drift/sqlite) with per-wallet isolation.
version: 1.0.0
publish_to: none
repository: https://github.com/siliconnexus-jp/KT-Wallet

environment:
  sdk: ^3.12.2

dependencies:
  drift: ^2.34.3
  sqlite3: ^3.5.0

dev_dependencies:
  build_runner: ^2.4.0
  drift_dev: ^2.34.0
  lints: ^6.0.0
  test: ^1.25.6

resolution: workspace
