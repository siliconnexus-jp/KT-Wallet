name: wallet_data
description: KT Wallet online-app persistence (drift/sqlite) with per-wallet isolation.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  drift: ^2.20.0
  sqlite3: ^2.4.0

dev_dependencies:
  build_runner: ^2.4.0
  drift_dev: ^2.20.0
  lints: ^6.0.0
  test: ^1.25.6

resolution: workspace
