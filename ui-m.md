# KT Wallet 双形态钱包需求文档（V2）

## 1. 项目名称

KT Wallet

中文暂定名：双形态钱包（普通钱包 + 双机离线钱包）

---

## 2. 产品定位

本产品由两个独立 App 组成：

1. KT Wallet 联网钱包 App
2. Cold Signer 离线签名 App

KT Wallet 是一个多钱包管理器，支持两种钱包形态：

### 2.1 普通钱包（热钱包）

* 助记词在联网设备本地生成或导入，经系统安全区加密保存。
* 本机直接签名并广播交易，无需二维码。
* 使用体验对标 OKX Wallet：低摩擦、高效率。
* 适合小额、日常、高频使用。

### 2.2 离线钱包组合（观察钱包 + 签名钱包）

* 联网端为观察钱包：仅持有公开地址，配对某个 Cold Signer 钱包。
* 签名由离线手机的 Cold Signer 完成，通过二维码传输。
* 私钥和助记词只存在于离线设备，不进入联网设备，不上传服务器。
* 适合大额、低频、高安全场景。

两种钱包在 KT Wallet 内使用同一个钱包列表统一管理，可随时来回切换。

产品定位为：

“一个 App 同时满足日常热钱包和双机离线冷签名的多钱包产品。”

本产品不宣称等同于专用硬件钱包。

---

## 3. 产品目标

V1 支持以下网络：

* Ethereum
* Polygon
* TRON
* Solana

V1 支持以下资产操作：

* ETH 转账
* Polygon 原生币转账
* ERC-20 Token 转账
* TRX 转账
* TRC-20 Token 转账
* SOL 转账
* SPL Token 转账

普通钱包与观察钱包支持相同的链与资产范围，仅签名路径不同。

V1 多钱包目标：

* 可创建多个普通钱包
* 可导入多个普通钱包
* 可连接多个观察钱包
* 钱包之间可随时切换
* 钱包数量上限 20 个

V1 暂不支持：

* Swap
* NFT
* Staking
* 跨链
* WalletConnect
* DApp Browser
* 任意合约调用
* Token Approve
* EIP-712 签名
* 多签钱包
* 批量转账
* Account Abstraction
* Solana 任意 Program 调用
* 私钥直接导入
* Keystore 文件导入
* 钱包类型互转（普通钱包与观察钱包创建后不可转换）

---

## 4. 用户角色

### 4.1 普通钱包用户

使用日常联网手机，追求效率。

主要需求：

* 快速创建或导入钱包
* 查看资产和币价
* 转账并本机直接签名
* 收款
* 查看交易记录
* 在多个钱包之间切换

### 4.2 观察钱包用户

使用日常联网手机，管理离线钱包的资产。

主要需求：

* 连接离线钱包
* 查看资产、币价、交易记录
* 创建转账
* 生成待签名二维码
* 扫描签名结果
* 广播交易

### 4.3 离线签名用户

使用长期保持离线状态的专用手机。

主要需求：

* 创建或导入钱包
* 备份助记词
* 导出公开地址
* 扫描待签名交易
* 查看真实交易内容
* 确认并签名
* 导出已签名交易二维码

---

## 5. 产品组成

### 5.1 联网钱包 App

名称：KT Wallet

主要模块：

* 钱包首页
* 钱包切换器
* 添加钱包
* 钱包管理
* 助记词备份（普通钱包）
* 身份验证（普通钱包签名）
* 资产列表
* Token 详情
* 转账
* 收款
* 交易记录
* 待签名二维码（观察钱包）
* 扫描签名结果（观察钱包）
* 广播状态
* 地址管理
* 网络设置
* 安全设置

### 5.2 离线签名 App

名称：Cold Signer

主要模块：

* 离线安全检查
* 创建钱包
* 导入钱包
* 助记词备份
* 地址导出
* 扫描待签名交易
* 交易解析
* 交易确认
* 生物识别验证
* 签名结果二维码
* 钱包管理
* 安全设置

---

# 6. 多钱包管理

## 6.1 钱包列表

每个钱包包含：

* 头像（颜色 + 名称首字）
* 钱包名称
* 钱包类型徽章：普通 / 观察
* 总资产估值
* 备份状态（普通钱包未备份时显示红点）
* Wallet ID

规则：

* 钱包数量上限 20 个。
* 列表按用户自定义顺序排列，支持在编辑模式中排序。
* 观察钱包徽章需明显区分，表明“本机无法签名”。

## 6.2 钱包切换器

入口：

* 首页顶部钱包名称，点按后从底部弹出半屏切换器。

切换器内容：

* 钱包列表（头像、名称、类型徽章、估值、备份红点）
* 当前钱包显示选中标记
* 底部“添加钱包”按钮
* “管理”入口进入钱包管理页

切换行为：

* 切换全局生效。
* 首页、资产列表、交易记录、收款页全部跟随当前钱包。
* 切换动作即时完成，无需二次确认。

## 6.3 添加钱包

三个入口：

1. 创建新钱包（普通钱包）
2. 导入助记词（普通钱包，支持 12 / 18 / 24 个单词）
3. 连接离线钱包（观察钱包，扫描 Cold Signer 导出的地址二维码）

V1 不支持：

* 私钥导入
* Keystore 导入
* 硬件钱包导入

## 6.4 钱包管理与编辑

单个钱包支持：

* 重命名
* 更换头像颜色
* 查看助记词（仅普通钱包，需身份验证）
* 备份提醒（仅普通钱包，未备份时展示）
* 删除钱包

删除规则：

* 普通钱包删除：需身份验证 + 二次确认，并再次提示助记词是否已备份。
* 观察钱包删除：仅移除本地公开地址与记录，不影响离线设备与链上资产。

## 6.5 数据隔离

以下数据按 Wallet ID 隔离存储：

* 交易记录
* 地址簿备注
* Token 显示 / 隐藏配置
* 本地转账备注

全局共享数据：

* 法币单位
* 显示语言
* RPC 节点配置
* App 锁设置

---

# 7. 核心业务流程

## 7.1 创建普通钱包

流程：

1. 用户在添加钱包中选择“创建新钱包”。
2. 展示热钱包安全提示：“热钱包适合小额日常使用，大额资产建议使用离线钱包组合。”
3. App 在本机生成助记词。
4. 提示用户手写备份，禁止截图。
5. 用户完成助记词校验。
6. 首次创建时设置 App 锁（密码 + 生物识别）。
7. 派生四条链地址，创建完成。

规则：

* 允许“稍后备份”跳过第 4-5 步。
* 未备份钱包在首页和钱包列表持续显示备份提醒。
* 未备份钱包发起转账前强制提示备份风险。

## 7.2 导入普通钱包

流程：

1. 用户选择“导入助记词”。
2. 选择助记词长度（12 / 18 / 24）。
3. 输入助记词，逐词校验 BIP-39 有效性。
4. 派生四条链地址。
5. 完成导入，标记为“已备份”。

规则：

* 粘贴导入是剪贴板的唯一例外，粘贴解析后立即清空剪贴板。

## 7.3 普通钱包转账

流程：

1. 选择资产，点击转账。
2. 输入收款地址和金额。
3. 选择手续费等级。
4. 查看交易确认页。
5. 点击“确认转账”。
6. 完成生物识别或密码验证。
7. App 在本机签名。
8. App 自动广播交易。
9. 展示交易 Hash 和状态。

规则：

* 每次转账必须完成身份验证。
* 全程无二维码环节。
* 签名或广播失败需展示明确错误状态并允许重试。

## 7.4 创建离线钱包（Cold Signer）

用户在离线手机中打开 Cold Signer。

流程：

1. 展示产品说明。
2. 提醒用户开启飞行模式。
3. 检测设备是否存在网络连接。
4. 用户选择“创建新钱包”。
5. App 本地生成助记词。
6. 用户确认已手写备份。
7. 用户完成助记词校验。
8. 设置 App 解锁密码。
9. 设置 Face ID 或 Touch ID。
10. 创建钱包完成。
11. 展示各链公开地址。
12. 用户通过二维码导出地址到 KT Wallet。

页面：

* 欢迎页
* 离线环境检查页
* 创建或导入钱包页
* 安全提示页
* 助记词展示页
* 助记词验证页
* 设置密码页
* 设置生物识别页
* 钱包创建成功页
* 地址导出页

## 7.5 导入已有钱包（Cold Signer）

流程：

1. 用户选择“导入钱包”。
2. 输入助记词。
3. 选择助记词长度。
4. 检查助记词格式。
5. 设置密码和生物识别。
6. 派生四条链的地址。
7. 用户确认地址。
8. 完成导入。

支持：

* 12 个单词
* 18 个单词
* 24 个单词

V1 不支持：

* 私钥直接导入
* Keystore 文件
* Ledger 导入
* 多组派生路径选择
* 自定义 Passphrase

## 7.6 连接观察钱包

KT Wallet 中不为观察钱包创建助记词。

流程：

1. 用户在添加钱包中选择“连接离线钱包”。
2. KT Wallet 打开扫描器。
3. 扫描离线钱包导出的账户二维码。
4. 展示导入的网络和地址。
5. 用户确认。
6. 创建观察钱包并加入钱包列表。
7. 开始查询余额和交易记录。

导入内容：

* 钱包名称
* Wallet ID
* 网络
* 地址
* 派生路径
* 账户索引
* 协议版本

联网设备不得通过此流程接收：

* 助记词
* 私钥
* Seed
* 可用于派生私钥的数据

若二维码中检测到上述数据，导入必须被拒绝并警告。

## 7.7 观察钱包转账（三段式）

### 第一步：联网端创建交易

用户在 KT Wallet 中：

1. 切换到观察钱包，选择资产。
2. 点击转账。
3. 输入收款地址。
4. 输入金额。
5. 选择手续费。
6. 查看交易概要。
7. 点击“生成签名请求”。
8. App 构建未签名交易。
9. App 生成动态二维码。

### 第二步：离线端扫描交易

用户在离线钱包中：

1. 点击“扫描交易”。
2. 扫描联网手机上的动态二维码。
3. 离线端重组完整数据。
4. 离线端独立解析原始交易。
5. 展示交易真实内容。
6. 用户确认地址、金额、网络和手续费。
7. 用户点击“确认签名”。
8. 完成密码或生物识别。
9. 离线端完成签名。
10. 展示签名结果动态二维码。

### 第三步：联网端广播交易

用户在 KT Wallet 中：

1. 点击“扫描签名结果”。
2. 扫描离线手机二维码。
3. 联网端验证签名结果。
4. 展示最终交易内容。
5. 用户点击“广播交易”。
6. App 提交到对应区块链网络。
7. 展示交易 Hash 和状态。

---

# 8. 联网钱包信息架构

## 8.1 钱包首页

展示：

* 当前钱包名称 + 类型徽章（点按打开钱包切换器）
* 总资产估值
* 24 小时资产变化
* 各网络资产
* 常用操作
* 最近交易
* 备份提醒横幅（普通钱包未备份时）

主要操作（按钱包类型变化）：

* 普通钱包：收款、转账、记录、更多
* 观察钱包：收款、转账、扫描签名结果、记录

首页状态：

* 正常
* 正在加载
* RPC 异常
* 无网络
* 尚未添加任何钱包
* 部分网络不可用

## 8.2 钱包切换器

半屏 Bottom Sheet。

展示：

* 钱包列表（头像、名称、类型徽章、估值、备份红点）
* 当前钱包选中标记

主要操作：

* 点按切换钱包
* 添加钱包
* 进入钱包管理

## 8.3 添加钱包页

展示三个入口卡片：

* 创建新钱包（普通钱包）
* 导入助记词（普通钱包）
* 连接离线钱包（观察钱包）

每个入口需说明钱包类型与安全等级差异。

## 8.4 钱包管理页

展示：

* 全部钱包列表（可排序）
* 每个钱包的类型、估值、备份状态

主要操作：

* 进入单个钱包详情
* 排序
* 添加钱包

## 8.5 钱包详情与编辑页

展示：

* 钱包名称
* 头像颜色
* 钱包类型
* Wallet ID
* 备份状态（普通钱包）
* 关联的 Cold Signer 信息（观察钱包）

主要操作：

* 重命名
* 更换头像颜色
* 查看助记词（普通钱包，需身份验证）
* 立即备份（普通钱包未备份时）
* 删除钱包

## 8.6 助记词备份页（普通钱包）

三个页面，联网端浅色主题：

1. 安全提示页：热钱包风险声明 + 手写备份要求 + 禁止截图声明。
2. 助记词展示页：12 词栅格，禁用截图，提供“稍后备份”。
3. 助记词校验页：抽查 2-3 个单词。

导入场景增加：

* 助记词输入页：长度切换、逐词 BIP-39 校验、候选词提示。

## 8.7 资产列表

每个资产项展示：

* Token 图标
* Token 名称
* Token Symbol
* 所属网络
* Token 数量
* 法币估值
* 24 小时涨跌

筛选：

* 全部
* Ethereum
* Polygon
* TRON
* Solana

搜索：

* Token 名称
* Symbol
* 合约地址

## 8.8 Token 详情页

展示：

* Token 名称
* 网络
* 余额
* 法币估值
* 价格
* 24 小时涨跌
* 合约地址
* 最近交易

主要操作：

* 转账
* 收款
* 查看区块浏览器

## 8.9 转账页

输入项：

* 网络
* 发送地址
* 收款地址
* 金额
* Token
* 手续费等级
* 备注，仅本地保存

支持：

* 扫描收款二维码
* 粘贴地址
* 地址格式校验
* 最大金额
* 自定义手续费

底部按钮：

“下一步”

## 8.10 交易确认页

展示：

* 网络
* 资产
* 转出地址
* 收款地址
* 转账金额
* 网络手续费
* 最大可能手续费
* 总支出
* 地址风险提示
* 交易有效期

主要按钮按钱包类型分叉：

* 普通钱包：“确认转账”（进入身份验证后本机签名并广播）
* 观察钱包：“生成待签名二维码”

观察钱包需额外提示：“该交易将由离线设备签名。”

风险情况：

* 收款地址格式异常
* 余额不足
* 手续费不足
* Token 合约未知
* 交易金额过大
* RPC 返回异常
* Solana Blockhash 即将过期
* TRON Energy 不足
* 普通钱包未备份助记词

## 8.11 转账身份验证页（普通钱包）

签名前要求：

* Face ID / Touch ID 或 App 密码

规则：

* 每次转账必须验证。
* 验证通过后本机完成签名并自动广播。
* 验证失败可重试，连续失败进入锁定冷却。

## 8.12 待签名二维码页（仅观察钱包）

展示：

* 动态二维码
* 当前分片进度
* 网络
* 金额
* 收款地址缩略信息
* 请求 ID
* 过期倒计时

主要操作：

* 暂停动画
* 调整二维码速度
* 调整二维码亮度
* 取消交易

提示文案：

“请使用离线签名手机扫描此二维码。”

安全提示：

“二维码中不包含助记词或私钥。”

## 8.13 扫描签名结果页（仅观察钱包）

展示：

* 摄像头扫描区域
* 分片扫描进度
* 当前交易请求 ID
* 取消按钮

扫描完成后：

* 校验请求 ID
* 校验钱包 ID
* 校验网络
* 校验签名者
* 校验交易内容
* 校验交易是否过期

校验成功后进入最终确认页。

## 8.14 广播确认页（仅观察钱包）

普通钱包签名后自动广播，不经过本页。

展示：

* 网络
* Token
* 金额
* 收款地址
* 网络手续费
* 签名地址
* 请求 ID
* 交易 Hash 预览

主要按钮：

“广播交易”

次要按钮：

“暂不广播”

## 8.15 广播结果页

两种钱包共用。

成功状态展示：

* 交易已提交
* 交易 Hash
* 当前状态
* 查看区块浏览器
* 返回首页

状态：

* 等待广播
* 已提交
* 确认中
* 已确认
* 失败
* 已过期
* RPC 拒绝
* Nonce 冲突
* Blockhash 过期

---

# 9. 离线签名 App 信息架构

## 9.1 离线首页

展示：

* 钱包名称
* 当前离线状态
* 最近签名记录
* 已启用网络
* 安全状态

主要操作：

* 扫描交易
* 导出地址
* 钱包管理
* 安全检查

首页不展示：

* 币价
* 资产估值
* 在线余额
* 网络数据

## 9.2 离线安全检查页

检查项：

* 飞行模式
* Wi-Fi 状态
* 蜂窝网络状态
* 蓝牙状态
* AirDrop 状态
* 是否启用设备密码
* 是否启用生物识别
* 是否存在屏幕录制
* App 完整性状态

状态等级：

* 安全
* 存在风险
* 禁止签名

严重风险时禁止继续签名。

示例：

“检测到当前设备存在网络连接，请关闭所有网络后再继续。”

## 9.3 扫描待签名交易页

展示：

* 摄像头扫描框
* 扫描分片数量
* 扫描进度
* 请求来源说明
* 取消按钮

二维码解析限制：

* 只接受支持的协议
* 只接受允许的数据类型
* 限制总数据大小
* 限制分片数量
* 拒绝未知版本
* 拒绝过期请求
* 拒绝重复请求

## 9.4 交易解析结果页

该页面必须由离线端根据原始交易独立生成。

通用展示内容：

* 网络
* 操作类型
* 转出账户
* 收款地址
* 金额
* Token 合约地址
* 原始 Token 数量
* 手续费
* 最大手续费
* 交易有效期
* 风险提示
* 请求 ID

主要操作：

* 拒绝
* 确认签名

## 9.5 ETH / Polygon 交易确认

原生币转账展示：

* 网络名称
* Chain ID
* From
* To
* Amount
* Nonce
* Gas Limit
* Max Fee
* Priority Fee
* 最大手续费
* 总支出

ERC-20 展示：

* 网络
* 操作类型：Token Transfer
* Token 合约地址
* From
* To
* 原始 Token 数量
* Token Symbol
* Token 精度
* Gas 信息

未知 Token 时：

* 不显示未经验证的 Token 名称
* 显示合约地址
* 显示原始数量
* 显示高风险警告

V1 遇到以下调用时拒绝签名：

* approve
* permit
* setApprovalForAll
* delegatecall
* 任意未知 calldata
* 任意合约调用

## 9.6 TRON 交易确认

TRX 转账展示：

* 网络
* From
* To
* TRX 数量
* 原始 SUN 数量
* Expiration
* Reference Block
* Permission ID
* 预计资源消耗

TRC-20 转账展示：

* Token 合约地址
* From
* To
* Token 原始数量
* Fee Limit
* Energy 风险提示
* Expiration

V1 遇到未知合约调用时拒绝签名。

## 9.7 Solana 交易确认

SOL 转账展示：

* 网络
* Fee Payer
* From
* To
* SOL 数量
* Lamports 数量
* Recent Blockhash
* 预计手续费
* 是否使用 Durable Nonce

SPL Token 展示：

* Mint 地址
* Source Token Account
* Destination Token Account
* Owner
* Token 数量
* 原始数量
* Associated Token Account 创建信息
* Program ID
* Compute Budget
* Priority Fee

V1 仅允许：

* System Program Transfer
* SPL Token Transfer
* SPL Token TransferChecked
* Associated Token Account 创建
* Compute Budget
* Durable Nonce Advance

存在未知 Program 时拒绝签名。

## 9.8 身份验证页

签名前要求：

* App 密码
* Face ID
* Touch ID

可配置策略：

* 每次签名验证
* 进入 App 时验证
* 超过一定金额二次确认

V1 默认：

“每次签名必须进行生物识别或设备密码验证。”

## 9.9 签名结果页

展示：

* 签名成功
* 网络
* 金额
* 收款地址
* 请求 ID
* 已签名交易 Hash
* 动态二维码

主要提示：

“请使用联网钱包扫描签名结果。”

操作：

* 调整二维码速度
* 暂停二维码
* 完成
* 作废本次签名

离线端不得提供：

* 复制原始签名
* 保存二维码到相册
* 分享二维码
* 导出文件到云端

## 9.10 地址导出页

展示所有公开账户：

* Ethereum 地址
* Polygon 地址
* TRON 地址
* Solana 地址

操作：

* 单独导出某个地址
* 批量导出全部地址
* 展示二维码
* 地址人工核对

二维码内容包含：

* 协议版本
* Wallet ID
* 账户名称
* 网络
* 地址
* 派生路径
* 账户索引

---

# 10. 风险提示设计

## 10.1 风险等级

### 普通

使用中性色提示。

示例：

“请核对收款地址和转账金额。”

### 警告

使用黄色或橙色。

示例：

“该 Token 未包含在离线钱包的可信列表中。”

### 高风险

使用红色。

示例：

“离线钱包无法识别该交易内容，已禁止签名。”

## 10.2 钱包类型安全分层提示

* 钱包类型徽章本身即安全等级标识：观察钱包 = 高安全，普通钱包 = 便捷。
* 创建普通钱包时必须展示：“热钱包适合小额日常使用，大额资产建议使用离线钱包组合。”
* 普通钱包未备份时，转账确认页必须展示备份风险提示。
* 观察钱包页面中出现签名相关操作时，必须提示“将由离线设备签名”。

## 10.3 地址展示规则

地址禁止只显示昵称。

至少展示：

* 前 8 位
* 后 8 位
* 完整地址查看入口
* 二维码入口

确认页建议默认展示完整地址，允许自动换行。

不能仅显示：

“发送给 Alice”

必须显示：

“Alice
0x1234...ABCD”

## 10.4 金额展示规则

同时展示：

* 用户可读金额
* 原始链上单位
* 法币估值，仅联网端
* 最大手续费

示例：

“10 USDT
10,000,000 原始单位”

---

# 11. 页面状态要求

所有核心页面需要设计以下状态：

* 初始状态
* 加载状态
* 成功状态
* 空状态
* 错误状态
* 超时状态
* 网络异常
* 数据过期
* 扫描失败
* 解析失败
* 用户取消
* 安全检查不通过
* 请求已签名
* 请求已过期

---

# 12. 二维码交互要求

二维码交互仅存在于观察钱包流程与地址导出流程。

采用动态二维码。

界面需包含：

* 扫描进度
* 分片总数
* 当前已识别分片
* 重复分片提示
* 数据校验结果
* 二维码播放速度
* 屏幕亮度提示

建议提供三个速度：

* 慢
* 标准
* 快

二维码页面禁止：

* 截图后继续使用作为默认流程
* 保存到相册
* 通过聊天软件发送
* 上传云端

---

# 13. 安全需求

## 13.1 离线设备

必须满足：

* 助记词只在离线设备生成或输入
* 助记词不得上传
* 助记词不得进入剪贴板
* 助记词不得保存到相册
* 私钥不得导出
* 不集成广告 SDK
* 不集成统计 SDK
* 不集成推送
* 不集成 WebView
* 不集成第三方登录
* 不包含行情模块
* 不访问网络接口

## 13.2 联网设备分层安全模型

按钱包类型分层约束：

### 观察钱包（红线不变）

联网设备不得存储或接收其关联的：

* 助记词
* 私钥
* Seed
* 助记词加密备份
* 离线设备解锁密码

### 普通钱包

* 助记词允许保存在本机，但必须经系统安全区加密（iOS Keychain / Secure Enclave，Android Keystore / StrongBox）。
* 助记词和私钥不得上传服务器。
* 不得明文落盘。

## 13.3 普通钱包密钥安全

* 助记词展示页禁用系统截图与录屏。
* 助记词不得进入剪贴板；导入时粘贴为唯一例外，解析后立即清空剪贴板。
* 助记词不得保存到相册或云端。
* 查看助记词必须通过身份验证。
* 每次转账签名必须完成生物识别或密码验证。
* 连续验证失败进入锁定冷却。
* 删除普通钱包前必须再次提示备份状态。

## 13.4 签名要求（离线端）

离线端必须：

* 独立解析交易
* 显示实际签名内容
* 校验网络
* 校验 From 地址
* 校验派生路径
* 校验 Wallet ID
* 校验请求 ID
* 校验交易有效期
* 拒绝未知交易类型

## 13.5 防重放

每个签名请求必须包含唯一 Request ID。

离线端记录：

* 已扫描
* 已拒绝
* 已签名
* 已过期

同一个 Request ID 默认只允许签名一次。

普通钱包本机签名同样为每笔交易生成唯一 Request ID，用于本地记录与状态跟踪。

---

# 14. 本地记录

## 14.1 离线签名记录

仅保存非敏感信息：

* 请求 ID
* 日期
* 网络
* 操作类型
* 收款地址
* 金额
* 签名结果状态

不得保存：

* 私钥
* 助记词
* 完整 Seed
* 解密后的密钥材料

## 14.2 联网交易记录

按 Wallet ID 隔离存储。

保存：

* Wallet ID
* 交易 Hash
* 网络
* Token
* 金额
* From
* To
* 状态
* 签名方式（本机签名 / 离线签名）
* 创建时间
* 广播时间
* 请求 ID

---

# 15. 设置页面

## 15.1 联网钱包设置

全局设置：

* 法币单位
* 显示语言
* RPC 节点
* 区块浏览器
* App 锁

按钱包设置（跟随当前钱包）：

* 钱包管理（重命名、头像、备份、删除）
* Token 管理
* 地址管理
* 隐私设置

## 15.2 离线钱包设置

包含：

* 钱包名称
* 生物识别
* 修改 App 密码
* 自动锁定时间
* 签名记录
* 导出公开地址
* 助记词备份验证
* 安全检查
* 删除钱包
* 销毁钱包数据

删除钱包必须：

1. 输入密码。
2. 完成生物识别。
3. 输入指定确认文字。
4. 再次提示助记词是否已备份。
5. 清除本地密钥数据。

---

# 16. 视觉设计建议

## 16.1 联网钱包（KT Wallet）

风格建议：

* 对标 OKX Wallet 等主流资产管理 App
* 信息丰富，强调操作效率
* 支持币价和图表
* 明确区分不同网络
* 钱包切换器交互轻量、快速
* 钱包类型徽章全局一致：普通钱包与观察钱包一眼可辨
* 观察钱包相关页面延续现有二维码流程视觉

## 16.2 离线签名钱包（Cold Signer）

风格建议：

* 极简
* 高对比度
* 少动画
* 无行情信息
* 无营销内容
* 强调安全检查
* 强调地址和金额
* 确认页避免视觉干扰

建议颜色语义：

* 绿色：验证通过
* 黄色：需要注意
* 红色：禁止签名
* 蓝色：普通操作
* 灰色：离线说明和辅助信息

---

# 17. V1 页面清单

## 联网钱包 App（KT Wallet）

1. 启动页
2. 添加钱包页
3. 创建钱包安全提示页
4. 助记词展示页
5. 助记词校验页
6. 助记词输入页（导入）
7. 扫描账户二维码页（仅观察钱包）
8. 导入确认页（仅观察钱包）
9. 钱包首页
10. 钱包切换器
11. 钱包管理页
12. 钱包详情与编辑页
13. 资产列表页
14. Token 详情页
15. 收款页
16. 转账输入页
17. 手续费选择页
18. 交易确认页
19. 转账身份验证页（仅普通钱包）
20. 待签名二维码页（仅观察钱包）
21. 扫描签名结果页（仅观察钱包）
22. 最终广播确认页（仅观察钱包）
23. 广播结果页
24. 交易详情页
25. 地址管理页
26. Token 管理页
27. 网络设置页
28. 安全设置页

## 离线签名 App（Cold Signer）

1. 启动页
2. 欢迎页
3. 离线安全检查页
4. 创建或导入钱包页
5. 助记词安全提示页
6. 助记词展示页
7. 助记词校验页
8. 助记词输入页
9. 设置密码页
10. 生物识别设置页
11. 钱包创建成功页
12. 离线首页
13. 地址列表页
14. 地址导出二维码页
15. 扫描交易页
16. 扫描进度页
17. 交易解析页
18. 风险警告页
19. 签名确认页
20. 身份验证页
21. 签名结果二维码页
22. 签名记录页
23. 钱包管理页
24. 安全设置页
25. 删除钱包页

---

# 18. V1 验收标准

满足以下条件即可认为 V1 核心流程完成：

### 多钱包管理

1. 可创建普通钱包并完成助记词备份校验。
2. 可通过助记词导入普通钱包。
3. 可连接观察钱包。
4. 钱包列表支持普通钱包与观察钱包混排，可随时切换。
5. 切换钱包后，首页、资产、交易记录、收款全部跟随当前钱包。
6. 交易记录、Token 配置、地址簿按钱包隔离。
7. 删除普通钱包必须经过身份验证与备份确认。

### 普通钱包

8. 普通钱包可构建支持范围内的转账。
9. 每次转账必须通过生物识别或密码验证。
10. 普通钱包可本机签名并自动广播，全程无二维码。
11. 普通钱包助记词经系统安全区加密存储，不上传、不明文落盘。

### 离线钱包组合

12. 离线手机可创建助记词。
13. 离线手机可导出四条链的公开地址。
14. 联网手机可查询四条链的资产余额。
15. 观察钱包可构建支持范围内的转账。
16. 未签名交易可通过动态二维码传输。
17. 离线端可独立解析交易。
18. 离线端可准确展示地址、金额、网络和手续费。
19. 离线端可拒绝未知交易。
20. 离线端可完成本地签名。
21. 签名结果可通过动态二维码返回。
22. 联网端可验证并广播交易。
23. 观察钱包关联的助记词和私钥全流程不进入联网设备。
24. 离线 App 不发起任何网络请求。
25. 同一个签名请求不能被无提示重复签名。
